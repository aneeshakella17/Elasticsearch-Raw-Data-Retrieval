#KibanaWebApp
This web app allows you to extract raw data from Kibana visualizations using the request query provided by Kibana.
To run on a specific IP, please change the IP field above to the IP of the device in which Kibana is running. Then,
execute python parse.py.